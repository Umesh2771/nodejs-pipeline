name : CI/CD Pipeline

on:
  push;
    branches;
  pull_request;
    branches;
    -main

    jobs;
    build;
        run-on; windows-latest

        steps;
          - name; Checkout code
            uses; action/checkout@v3


